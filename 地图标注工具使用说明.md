# 地图标注工具使用说明

打开 `pages/map-editor.html`。这是开发工具，不需要登录，不修改玩家存档。

## 编辑

1. 左上角选择地图，画布显示原图与现有标注。滚轮缩放，按空格拖动或按鼠标中键平移。
2. 红色是碰撞区域。选择“画碰撞区域”后在图上按住拖动绘制。用“选择 / 拖动”移动已有矩形，拖右下角白色方块缩放，也能在右侧精确输入坐标和尺寸。
3. 绿色是允许站立的区域。整张图没有绿色标注时只按碰撞区域限制；存在绿色标注时，人物脚底圆必须完全处于绿色区域。连接通道要留足宽度，不要只让矩形边缘刚好相接。
4. 黄色是交互点，小圆是位置，大圆是交互范围。选中后可以拖动或调整半径。已有 ID 不可改名，避免破坏剧情引用；修改坐标不会删除隐藏条件和其他事件字段。
5. 蓝色是出生点。可拖动，或使用“设置出生点”在地面点击。
6. 出口要指定目标房间和目标落点。点击“在目标地图上点选落点”，工具会切到目标地图；点击可站立地面完成设置。正向出口和反向出口分别配置，不会自动添加另一扇门。
7. “添加剧情点”需要从右侧选择已有剧情节点，必要时填写进度条件。这只是设置触发入口，不会新建剧情正文。
8. 删除选中可删除矩形或交互点；出生点不可删除。支持撤销、重做与 Ctrl+Z / Ctrl+Shift+Z。

## 验证

“检查全部地图”核对出生点、目标落点、区域越界和交互范围。未通过时禁止本机保存和项目配置导出，仍可导出 JSON 备份草稿。

“试玩碰撞”用白色圆表示人物脚底，尺寸和移动速度与正式地图一致。点击地面设置位置，WASD / 方向键移动，Shift 疾跑。此模式只试碰撞，不触发剧情和出口。检查通过不代表所有通路连通，必须实际走一遍。

## 保存与交付

- **保存到本机**：保存在当前浏览器 localStorage，刷新同源游戏后应用。推荐从同一个本地静态服务打开工具和游戏；不同浏览器、端口及 file/HTTP 不共享配置。已经打开的游戏需要刷新。
- **导出 JSON**：备份或传给队友，在工具中“导入 JSON”继续编辑。导入后还需保存。
- **导出项目配置**：下载 `map-layout-data.js`，放进项目 `js/` 文件夹替换同名文件。正式游戏已加载这个文件，提交时带上即可。不会自动写入项目文件，避免浏览器覆盖错误路径。
- **优先级**：原始地图 → `js/map-layout-data.js` → 本机覆盖。检查队友配置时先清除自己的本机覆盖。
- **恢复当前地图原始配置**只改变编辑草稿，可撤销；**清除本机覆盖配置**保留项目配置和玩家存档。

## 范围

编辑的是正式探索的 8 张地图。第二场剧情播放器内的宿舍调查由 `dorm-exploration.js` 单独管理，不使用本工具配置。工具不修改地图图片、人物大小、剧情正文或分支规则。赵灵现有 NPC 站位随对应交互点同步。

# 地图素材

这里放可移动探索页使用的整张地图。地图需要配合 `js/map-art.js` 中的房间尺寸、碰撞区域、入口和交互点使用。

当前已接入的地图按用途保存在 `world/`（馆内总览）和 `rooms/`（可行走小场景）；后续图块、独立物件和遮挡前景分别放入 `tilesets/`、`props/`、`foregrounds/`，并同步 `js/asset-paths.js`。详见上级目录的《素材分类指南.md》。

地图不应该作为普通视觉小说对白的全屏背景。

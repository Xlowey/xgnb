# 剧情背景素材

这里放视觉小说播放器使用的单张剧情背景。它们只负责对白、独白、系统提示和事件演出，不承担角色移动、碰撞或入口逻辑。

当前已接入的剧情背景按地点保存在 `locations/` 下；同一地点的正常、异常、昼夜状态放在同一地点目录。细分后需同步 `js/asset-paths.js`、剧情配置和 CSS 引用。详见上级目录的《素材分类指南.md》。

如果某个地点还没有专用剧情视角，播放器会使用暗色主题底，不会偷偷回退到地图。

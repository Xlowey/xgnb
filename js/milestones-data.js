/*
 * 一次性发钱的里程碑（012 §4.2 的「每日存活」「幕结算」「玩法通关」都落在这张表上）。
 *
 * 一条 = 「某个旗标成立 → 发一笔，且只发一次」。幂等靠 `state.flags["milestone:<id>"]`，
 * 复用现成的旗标袋，**不加新变量、不改存档结构**。
 *
 * ★ 下面每一条的场次都是**对着 009 原文核过的**，不是照抄 012 的转述——012 §4.2
 *   转述的三个天数节点里有一个在 009 里根本不存在（见下）。
 *
 * ⚠️ **幕结算（012 §4.1 的 250 点）没有写进这张表**，因为 009 里**没有幕结构**：
 *   009 全 29 场的副标题都是「第二周剧本 · 第N场」，没有任何分幕标记。幕结构在 **006**
 *   （《游戏剧情文稿》），而且**是 11 幕不是 012 写的 10 幕**。要把幕结算接进来，
 *   得先把 006 的 11 幕映射到 009 的 29 场（每一幕结束在哪一场），那是另一件活。
 */
(function () {
  "use strict";

  window.MuseumMilestoneDefinitions = [
    // ---------- 每日存活（012 §4.2：三夜结束各 +20）----------
    // 009 里能站住的天数节点只有两个：
    //   第十二场「△ 回宿舍睡觉，梦里（CG）桃树的梦」    → 第一夜结束
    //   第二十三场「请现在返回宿舍等待第三天的到来」    → 第二夜结束
    //
    // 012 写的第三个节点「第十三场（第二天开始）」**在 009 里不存在**——第十三场整场是
    // 蜡像馆（张明诚的展台），没有任何天数内容。所以这里先只发两笔（40 点，不是 60）。
    // 第三笔等天数节点定下来再补一行；012 里另一个候选「第二十五场」标题是「午夜前」，
    // 那是第三天当晚的一个时刻，不是一夜的边界。
    { id: "survive-night-1", flag: "scene12Seen", points: 20, source: "存活过第一夜" },
    { id: "survive-night-2", flag: "scene23Seen", points: 20, source: "存活过第二夜" }
  ];
}());

# 重复素材：9.17/ 里 8 张图仓库内全有、逐字节相同（2026-09-20 用 md5 逐个核对过，0 个独有）。
# 留档不必入库，避免同一张图在仓库里存两份。
9.17/
9.17.7z

# 仓库规矩（一直靠人记，写进文件让 git 接手）：
# start.ps1 里有明文 key；后两项体积过大（8.8 GB / 31 MB），都不该进这个仓库。
start.ps1
参考优秀案例/
电影欣赏网站.zip
